package dev.mathops.assessment.expression.editmodel;

/**
 * Delimiters.
 */
public enum EDelimiter {

    /** Decimal point. */
    DECIMAL_POINT,

    /** Comma. */
    COMMA,

    /** Left Brace. */
    LEFT_BRACE,

    /** Right Brace. */
    RIGHT_BRACE,

    /** Left Bracket. */
    LEFT_BRACKET,

    /** Right Bracket. */
    RIGHT_BRACKET,

    /** Left Parenthesis. */
    LEFT_PAREN,

    /** Right Parenthesis. */
    RIGHT_PAREN,

    /** Left Quote. */
    LEFT_QUOTE,

    /** Right Quote. */
    RIGHT_QUOTE;
}
