/**
 * Snap-ins in the operations app. Each snap-in can present three views: a full-screen view, a "dashboard" tile, and a
 * thumbnail. The operations window will either show all dashboard tiles arranged in a grid, or will show one
 * full-screen view, and the rest as thumbnails plus a button to return to the dashboard view.
 */
package dev.mathops.app.ops.snapin;
