/**
 * A centralized Math Placement website where all access prior to login goes to a single page, and navigation within
 * that page is entirely JavaScript-based.
 *
 * <p>
 * This is to ensure that search engine links all go to the same central page, rather than a set of disparate pages that
 * can prevent people from finding information.
 */
package dev.mathops.web.site.placement.main;
