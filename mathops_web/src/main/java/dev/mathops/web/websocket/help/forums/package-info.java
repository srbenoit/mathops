/**
 * A web-socket service to manage a list of fora, each with posts from students and course assistants. Fora are public
 * spaces where all participants can see all other participants' posts.
 *
 * <p>
 * It is assumed that fora are reset for each class (each semester), but data for the entire current semester is
 * retained and visible to all participants.
 */
package dev.mathops.web.websocket.help.forums;
