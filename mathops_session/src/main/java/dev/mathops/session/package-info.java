/**
 * Provides session management functions and manages the set of active sessions. This package contains the various
 * authentication methods that can be used to validate logins and create sessions, and the logic to log in, log out,
 * validate session IDs, set roles and effective user IDs, and set presence values for logged-in users.
 */
package dev.mathops.session;
