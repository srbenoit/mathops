/**
 * Utility classes to lay out and render instances of document objects to Swing panels.
 *
 * <p>
 * This package includes a "DocColumnInstPanel" class that can display a {@code DocColumnInstPanel} and that will
 * respond to user actions such as key and mouse events to update inputs.
 */
package dev.mathops.assessment.document.swing;