/**
 * Provides business logic for use in constructing web pages.
 * <p>
 * Many of these objects support "gathering data" for a particular student and current time. Rather than storing such
 * data internally, these gather methods should generate immutable data objects that store the snapshot of the
 * underlying data for the particular student and time. In that way, the logic objects may be reused.
 */
package dev.mathops.session.sitelogic.servlet;
