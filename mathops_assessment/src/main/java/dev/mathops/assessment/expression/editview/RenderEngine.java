package dev.mathops.assessment.expression.editview;

import java.awt.Graphics2D;

/**
 * An engine that renders a laid out expression.
 */
public enum RenderEngine {
    ;

    /**
     * Renders a laid out expression.
     *
     * @param view the view to render
     * @param g2d the {@code Graphics2D} to which to draw
     * @param x the x position of the left edge of the rendered expression
     * @param y the y position of the typographic baseline
     */
    static void render(final ExprObjectView view, final Graphics2D g2d, final int x, final int y) {


    }
}
