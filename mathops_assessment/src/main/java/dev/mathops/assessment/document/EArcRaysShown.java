package dev.mathops.assessment.document;

/**
 * Styles for arc ray visibility.
 */
public enum EArcRaysShown {

    /** No rays - just arc is drawn. */
    NONE,

    /** Ray at start angle. */
    START,

    /** Ray at end angle. */
    END,

    /** Ray at both start and end angle. */
    BOTH,
}
